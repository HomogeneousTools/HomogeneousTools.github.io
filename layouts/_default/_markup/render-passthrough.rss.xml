{{- $opts := dict "displayMode" (eq .Type "block") "output" "mathml" -}}
{{- with try (transform.ToMath .Inner $opts) -}}
  {{- with .Err -}}
    {{- errorf "Unable to render mathematics: %s. See %s" . $.Position -}}
  {{- else -}}
    {{- .Value -}}
  {{- end -}}
{{- end -}}
